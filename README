-tornado-mongo-googleoauth
-=========================
-
-A Basic Example with Tornado and MongoDB with Google OAuth
-
-This is a starting package, from which you can easily expand based on your needs. Based on simple assumptions, here we used Google OAuth and basic pumongo driver, which is being accessed via self.db.users.find() as an example.
